/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you use next/image with external images, add domains here
  // images: { domains: [] },
}

module.exports = nextConfig
